packages:
  "framer-motion": "For smooth chat bubble animations and layout transitions"
  "react-markdown": "To render Gemini AI responses with formatting"
  "remark-gfm": "For github flavored markdown support in chat"
  "date-fns": "For formatting conversation timestamps"

images: []

notes:
  - "Using custom hook for SSE streaming since generated hook doesn't support EventSource with POST."
  - "Assuming role from DB is 'model' or 'assistant' for AI, 'user' for human."
